---\ntitle: "Daily Data-Intel Scrape Pipeline Cron"\ndescription: >-\n  Multi-step cron that scrapes external pg/Supabase source → classify freshness → deterministic 6-gate audit per entity (blocklist-filter enforced) → compound into vault Decisions/Topics + daily note w/cycle counter increment → deliver ProofPacket summary. Covers the load→scrape→process-audit-gates→compound-vault-output-delivery chain.\n\n# daily-data-intel-scrape-pipeline-cron\n\nA **data-ingestion cron** — distinct from the read-only analytics snapshot (#7, `read-only-analytics-cron.md`) because it *ingests* fresh entities (not snapshots state), freshness-classifies, individual-gate-audits every entity against blocklist/rules before it enters any persistent store, then compounds approved ones + delivers a ProofPacket.\n\n## The pipeline: load → scrape → process(audit gates) → compound(vault output) → deliver(proof)\n\n| Phase | Goal | Key discipline |\n| Load (data-01) | Queue/vault state; classify today vs stale source records | Disk-state verification BEFORE tool invocation (see pitfall §4 below). Do NOT re-execute steps that failed prior windows without verifying artifact-disk-state changed. |\n| Scrape (data-02) | Extract fresh entities from pgvector/Supabase sources (>50% freshness target vs stale classification cutoff) | Primary-source-only when design specifies it: direct-pgvec-endpoint-invocation, NOT fallback-metadata-APIs that return index-descriptions not vector-indexed actual-entity payloads suitable for downstream processing. |\n| Process (data-03) | Extract fields per entity; run deterministic 6-gate audit sequence each one passes/rejected explicit fail-reason-per-gate recorded so reruns discriminate retry-helpful-from-permanent-fail |\n| Compound (data-04) | Approved entities written to vault dirs labeled \"Decisions/Topics\" updated-in-daily-note-template-format with cycle-counter increment; output-counts logged (§3: zero-payload-runs MUST declare `entities_compounded=0` + differentiate themselves explicitly from successful-run marker, file-presence-alone not progress-signal) |\n| Deliver (data-05) | ProofPacket delivery to configured destination concisely (preference-captured-user-delivery-summary ≤6 lines maximum for confirmation-channel closure — enforce at deliver-phase always this-is-not-a-generalized-log but the user-preferred-proofsnapshot that closes the loop on this cron run specifically, not general executive summary). Session ends after delivery confirms success OR explicit blocker documented with verification steps required for human intervention when autonomous resolution exhausted all sources attempted AND diagnosed-real-blocker-not-guessed-something-wrong-without-checking.) |\n\n## Critical: disk-state inspection BEFORE tool invocation in every scheduled run (live-failure-learned 2026-07)\n\n**The compaction-summary bleed mode — a real-cost hazard this session paid for across + rounds:**\ncompacted context summaries contain stale task-lists + error-transcripts from earlier windows. Those read as reference history NOT today's instructions-but agents routinely re-execute terminal commands confirmed-bad-in-prior-windows (paths-missing scripts-misnamed-syntax-garbled-code-from-compaction-boundary-loss) without verifying disk reality first.\n\n**Mandatory step before ANY network/terminal command in this pipeline run:**\n1. Read most-recent daily log: `ls -t ~/Documents/JakeStudio/Logs/data-pipeline-YYYY-MM-DD.md | head -1` + inspect its tail (not full-read necessarily just check whether payload-counts show work-done-or-empty-run-vs-stale-narrative). Determines what actually-materialized between sessions regardless-how-authoritative previous-windows made their state appear when summarized.\n2. Verify expected executables exist AND names match reference: `ls ~/.hermes/bin/<expected-pattern>*` or search project-directories for script pattern (`rg "pipeline.*\\.py" ~/bin/ ~/Developer/<project>/scripts/ --files-with-matches`). File-not-found IS config-state-issue — find-correct-file-then-run; never re-attempt-wrong-name first.\n3. Inspect payload directory contents: `find ./results <payload-dir> -mtime 7` verify whether data-materialized-yet from claimed prior-attempts (prior failure-mode: WebFetch claims wrote files there but content-body was NEVER inspected-in-situ only metadata-dummy-frames written counted-as-successful-output). Never trust file-existence-stubs as evidence-of-completed-scrape — read at least body-content of one written-file to verify payload-shape before proceeding downstream phases that consume bad-input silently.\n\n## Zero-payload-run discipline (from class-skill Rule 3/Rule 7: auditable evidence chain)\n\nEmpty compounding run still writes a daily-entry but MUST contain:\n- explicit `entities_loaded/scraped/processed/gate_passed/compounded` counts (all zero if that's truth for today — better than omitting them making-log-presence-indistinguishable from small-batch-successful-run-looking-at-file-only-not-content)\n- rejected/reasons summary per audit gate so next session knows WHY fewer entities compounded vs raw-scrape-count suggests pipeline "lost" records mysteriously somewhere-without-trail-find\n- cycle-counter increment behavior: only increment AFTER actual-compound confirmed for ≥1 entity; zero-payload increments-inflate-progress-false-signal.\n\n## Blocker resolution path (when primary source unavailable)\nDocument verified blocker state before session end:\n- Supabase/pgvector endpoint unreachable? Verify exact endpoint URI + credential-store-path stored at known-location exists (do NOT guess-at-name — script-misname-confusion-across-windows compounded-not-verifying-disk-names-first).\n- Fallback sources tried AND confirmed-unusable-by-evidence: GitHub search/OpenAlex API return-only-index-metadata-not-vector-entity-payloads; log this evidence explicitly so next session knows fallback-already-diagnosed-not-yet-retried.\n\nNamed formulas without MathProofPacket verification are BLOCK (see mathexec gates doc + CLI). Gate-D public-action authority still flows through standard RIG procedure — autonomy of scheduled-run does NOT relax outward/costly-irreversible scope.\n